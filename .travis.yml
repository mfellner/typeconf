sudo: false
branches:
  only:
    - master
language: node_js
node_js:
  - "8.9.4"
  - "9.6.1"
cache:
  yarn: true
  directories:
    - node_modules
before_script:
  - yarn add codecov
script:
  - yarn test
after_success:
  - node ./node_modules/.bin/codecov -f coverage/*.json
