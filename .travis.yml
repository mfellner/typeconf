sudo: false
branches:
  only:
    - master
language: node_js
node_js:
  - "6.11.0"
  - "8.0.0"
cache:
  yarn: true
  directories:
    - node_modules
before_script:
  - yarn add codecov
script:
  - yarn test
after_success:
  - node ./node_modules/.bin/codecov -f coverage/*.json
